# 최강피자 홈페이지 리디자인 실행 계획

> 승인 시안: `디자인수정참고파일들/최강피자_홈페이지_전체스크롤_통합본.jpg` (최종본)
> 개별 파일명이 곧 섹션별 지시서입니다. 이 문서는 그 지시를 코드 작업으로 번역한 것입니다.
> 방식: **comp-led 재현 빌드** — 시안이 법(法)이고, 빌드는 시안과 겹쳐보며 맞춥니다.

---

## 0. 브리프 (확정)

| 항목 | 내용 |
|---|---|
| **방문자 모드** | Persuade — 방문자가 판단하고 행동한다 |
| **1순위 방문자** | 예비 가맹점주. 여러 프랜차이즈를 연달아 비교 중. 다수 모바일·카카오톡 인앱 유입 |
| **해야 할 행동** | 가맹 상담 전화(1866-1623) 또는 카카오톡 상담 |
| **믿어야 할 것** | "이 브랜드는 초기 부담을 본사가 먼저 덜어주고, 남는 돈을 숫자로 공개한다" |
| **증거** | 30호점 한정 혜택표 · 수익구조 원형차트 · 실제 매장 리뷰 17장 · 실제 주방 영상 3편 · 레드컨셉 매장 실사 |
| **성공** | 상담 문의 발생. 스크롤 도중 어디서 이탈해도 상담 진입이 한 탭 거리 |
| **손대지 않을 것** | 회사 정보·연락처·주소, `/menu` 라우트, SEO 구조화 데이터, 카카오 인앱 뷰포트 보정 로직 |
| **하지 말 것 (anti-goal)** | 시안에 없는 수치·후기·매장 수 창작 / 스톡 이미지 / 구 문구("20호점까지 800만원 지원" 등) 재사용 |

---

## 1. 섹션 매핑 — 무엇을 지우고, 무엇을 새로 만드는가

시안 파일명이 지시입니다. 아래가 그 전체 번역입니다.

| # | 새 섹션 | 시안 근거 | 현재 코드 | 처리 |
|---|---|---|---|---|
| S0 | 헤더 | `11`, `77` | `ported-header.tsx` | **재작성** — 히어로 위 투명 → 스크롤 시 네이비 솔리드. 골드 필 CTA "창업 상담 신청 →" 추가 |
| S1 | 히어로 · 맛도 최강 | `11-첫화면.png` | `ported-hero.tsx` | **전면 교체** |
| S2 | 30호점 가맹 혜택 | `22- 수정후.png` | `ported-value-strip.tsx` (강한 이유) | **삭제 후 신규** |
| S3 | 철학 · 모집하지 않습니다 | `33-수정후.png` | `contact-band` 성공비결 5카드 | **삭제 후 신규** |
| S4 | 수익 구조 | `44-수정후-.png` | `contact-band` shopInShopIntro | **삭제 후 신규** |
| S5 | 집중운영 시스템 | `77-중간 수정후.png` | `contact-band` threeWaySection | **개편** — 3카드 → 4카드, 네이비→옐로우 |
| S6 | 진짜 주방 (영상) | `99- 하단 수정 부분.png` | `ported-brand-video-section.tsx` | **재작성** — 3분할 세로 영상 카드 |
| S7 | 리뷰 증명 | `88(그데로, 파랑색만 밝은색으로).jpg` | `ported-review-showcase.tsx` | **유지 + 색만 교체** (네이비 → 밝은 브랜드 블루) |
| S8 | 가맹 문의 + 푸터 | `10(그데로 사용).jpg` | `contact-band` 하단 | **그대로 유지** |

### 삭제 확정 (시안이 명시)

- `5-수정전 (5번 삭제).jpg` → **가맹비용 3분할 섹션 삭제.** 내용은 S2 혜택표로 흡수. 구 수치(가맹비 500만/교육비 300만/평당 220만/집기 2,500~3,000만) 전량 폐기.
- `6-수정전(6번 삭제).jpg` → **최강샵인샵 "공간은 그대로, 수익만 더하세요" 섹션 삭제.**
- `첫화면 피자메뉴- 첫화면에서 빼도됨.jpg` → **홈에서 메뉴 그리드 제거**, 헤더 "메뉴" → `/menu`로 유도. (통합본에 메뉴 섹션 없음)
- `44-수정전` 최강 SHOP-IN-SHOP 인트로 → 삭제.
- `22-수정전` "최강피자 강한 이유" 4아이콘 스트립 → 삭제. 내용은 히어로 하단 3종 아이콘으로 흡수.

### 최종 스크롤 순서

```
헤더 → S1 히어로 → S2 가맹혜택 → S3 철학 → S4 수익구조 → S5 집중운영 → S6 진짜주방 → S7 리뷰 → S8 문의·푸터
```

배경 리듬 (의도적 명암 교대):
`블루 → 화이트 → 화이트↘블루 → 크림 → 옐로우 → 블루 → 라이트 → 다크`

> ⚠️ 현재 사이트는 전 구간 다크 네이비입니다. 새 시안은 **밝은 배경이 기본**이고 컬러 필드가 교대합니다. `globals.css`의 `color-scheme: dark`와 `body{background:#04122b}`가 전면 수정 대상입니다.

---

## 2. 디자인 토큰 — 시안에서 직접 추출한 값

ffmpeg 팔레트 추출 + 픽셀 샘플링으로 뽑은 실제 값입니다. 추정치 아님.

```css
/* 브랜드 블루 — 히어로/영상/리뷰 배경 */
--blue-hero:    #0032F0;  /* 11 히어로 바닥 */
--blue-band:    #0148E3;  /* 33 하단 블루 밴드 */
--blue-video:   #0133DD;  /* 99 진짜주방 바닥 */

/* 네이비 — 텍스트, 패널, 표 헤더, 스크롤 헤더 */
--navy-900:     #011750;
--navy-800:     #041342;
--navy-700:     #051839;
--navy-600:     #021B3D;

/* 골드/옐로우 */
--yellow-500:   #FDD816;  /* 77 섹션 바닥, CTA 버튼 */
--gold-400:     #FCD666;  /* 필/하이라이트 텍스트 */
--gold-300:     #FCE374;
--gold-600:     #E5BB53;  /* 헤어라인 룰 */

/* 오렌지 — S4 수익구조 전용 (골드와 다름, 혼용 금지) */
--orange-500:   #F27920;
--orange-400:   #F8A328;

/* 레드 — 강조 1회용 (전액 면제 / 집중운영 / 2억) */
--red-500:      #E12110;
--red-600:      #DB1412;

/* 중립 */
--white:        #FDFDFD;
--cream-card:   #F9F4EA;  /* 77 카드 */
--cream-ground: #FAF6F2;  /* 44 바닥 */
--gray-line:    #EBE9E8;
```

**색 전략:** Full palette (4개 역할색: 블루/골드/네이비/오렌지 + 레드 1회용). 페이지 단위로 필드가 통째 색을 갖고, 액센트를 흩뿌리지 않습니다.

**타이포:** 시안 헤드라인은 Pretendard보다 무겁고 각진 디스플레이체입니다. Pretendard 900은 근사치일 뿐입니다.
→ **결정 필요:** ① Pretendard 900으로 진행(추가 로딩 0, 시안과 미세 차이) ② 디스플레이 웹폰트 1종 추가(예: GmarketSans Bold / 에스코어드림 9) ③ 디자이너에게 원본 폰트명 확인. **③ → ② 순으로 권장.**
본문/UI는 Pretendard 유지.

---

## 3. 섹션별 상세 스펙

### S1 · 히어로 — `11-첫화면.png`
- 바닥 `--blue-hero`. 좌측에 거대한 왕관 심볼 고스트 워터마크(불투명도 낮게, 화면 밖으로 잘림).
- 좌: 골드 별 4개 → `맛도`(골드) `최강,`(화이트) / `재료도`(골드) `최강.`(화이트) — 초대형 2행. → 서브 `한 판의 기준을 만드는 브랜드, 최강피자.`(최강피자 골드) → 본문 2행 → 골드 라인아이콘 3종(잎/셰프모자/메달) `신선한 재료 / 정직한 조리 / 최고의 맛`, 세로 구분선.
- 우: 반반 피자 실사 풀블리드. **기존 자산 `hero-choigang-half-half-pizza.png`가 시안과 일치** — 재사용. 우상단 빨간 점선원 `BEST` 배지, 로즈마리 가지 2개, 부스러기 입자.
- 모션: 피자 아주 느린 회전 또는 진입 시 스케일업 1회. 별 4개 순차 등장.

### S2 · 30호점 가맹 혜택 — `22- 수정후.png`
- 화이트 바닥. 좌상단 로고. 중앙 네이비 필 `FRANCHISE`(골드 텍스트).
- 헤드라인: `딱` + **골드 브러시 스트로크 위에 얹힌** `30호점까지,` (딱 아래 빨간 밑줄) / 2행 `본사가 먼저 챙기는 실전형 가맹 혜택`(네이비). 브러시는 SVG path로 재현.
- 표: 네이비 헤더(구분/내용/혜택), 4행. 각 행 좌측에 네이비 원형 아이콘 칩(태그/트럭/공구/헤드셋).
  혜택열: `전액 면제`(레드+반짝) · `적용`(블루) · `직접 시공 가능 (조건 협의)`(블루) · `실전 지원`(블루).
  1·2행 우측에 골드 필 `30호점 특별혜택 ❶ / ❷`.
- 네이비 배너: 왕관 원형 + `✦ 30호점 한정 실전 혜택 ✦` / `가맹비 전액 면제 · 오픈 후 3개월 물류 지원` (화이트/골드/레드 혼합).
- 마스코트(엄지척)가 배너 좌하단에 걸쳐 앉음.
- 빨간 별표 각주 2줄.

### S3 · 철학 — `33-수정후.png`
- 상단 화이트 → 하단 `--blue-band` 밴드로 전환(카드가 경계를 가로지름).
- 블루 필 `✦ PHILOSOPHY ✦`(골드) → `단순히 가맹점을`(중간 굵기) / `모집하지 않습니다!`(초대형 네이비).
- 화이트 라운드 카드 2분할:
  - 좌: 블루 인용부호 → `함께 성장하고 / 함께 앞으로 나아갈 / 점주님들을 모집합니다.`(강조부 볼드) → 본문 2행. 우측에 마스코트 엄지척.
  - 우: **레드컨셉 매장 인테리어 실사** (`자료/레드컨셉.png` 상단 이미지 크롭 필요).
- 우측 플로팅 레일: `TALK`(검정 원) / 상담(검정 원) / `TOP`(화이트 원). — 이 레일은 S3부터 하단까지 상시 노출.

### S4 · 수익 구조 — `44-수정후-.png`  ★가장 복잡
- `--cream-ground` 바닥, 상단 좌우 골드 헤어라인.
- 헤드라인 `매출보다 중요한 건, 실제로 남는 [수익]입니다.` (수익=오렌지). 서브(수익 구조=오렌지).
- **좌: 도넛 차트 6분할** — 식자재 35~38%(골드) / 임대료 8~12%(골드) / 플랫폼 8~12%(슬레이트) / 인건비 12~18%(네이비) / 기타 운영비 8~15%(블루그레이) / 예상 영업이익 12~18%(오렌지). 각 조각에 아이콘+라벨+% 인라인.
  중앙 화이트 홀: 💰 `월 평균 매출 / 8,000만원 / *점포별 상이할 수 있습니다.`
  차트 좌측: 막대그래프 아이콘 + `예상 영업이익 / 12~18%`(초대형 오렌지) / `최대 예상 영업이익 범위`, 위아래 골드 룰.
  → **이미지가 아니라 SVG로 구현.** 크리스프하고 진입 시 조각이 순차로 그려지는 모션 가능.
- **우: 화이트 카드 표** — 네이비 탭 `수익 구조 한눈에 보기`. 항목/비율(매출 대비) 5행 + 네이비 원형 아이콘. 마지막 `예상 영업이익 12~18%` 오렌지 행.
  카드 하단 네이비 바: `우리 매장의 실제 수익이 궁금하다면?` + 오렌지 버튼 `수익 구조 상담 신청하기 ›`.
- 각주 1줄.
- 하단: 신뢰 카드 3종(검증된 수익 구조 / 안정적인 매출 시스템 / 본사와의 상생 파트너십), 네이비+골드 배지 아이콘.

### S5 · 집중운영 시스템 — `77-중간 수정후.png`
- `--yellow-500` 바닥 + 아주 옅은 격자.
- `최강피자 [집중운영] 시스템` (집중운영 = `--red-500`), 서브 1행.
- **크림 라운드 카드 4장**(현재 3장에서 확장): 집중 운영 시스템 / 배달 최적화 / 실전형 오픈 지원 / 운영 효율 상승.
  각 카드: 네이비 타이틀 → 골드 헤어라인 → 원형 크림 배경 위 일러스트 → 2~3행 캡션(강조부 볼드).
- 카드 사이 네이비 원형 연결자: `=` `+` `+`.
- ⚠️ 4개 원형 일러스트(과녁/배달 라이더/매장 악수/성장 그래프)는 **3D 렌더 원본이 필요**합니다. §5 참조.

### S6 · 진짜 주방 — `99- 하단 수정 부분.png`
- `--blue-video` 바닥.
- 좌: `최강피자의 / [진짜 주방을] / 보여드립니다.` (진짜 주방을 = 골드, 초대형) → 본문 2행 → 골드 아웃라인 아이콘 2종(다이아/방패체크) + `정직하게, 진심으로` / `기본을 지키고, 맛으로 증명합니다` 각 2행.
- 우: 세로 영상 카드 3장(9:16). 다크 오버레이 + 중앙 재생 버튼 + 골드 `— 01/02/03` + 화이트 볼드 2행 타이틀(`푸짐하게 올리는 토핑` / `기본부터 제대로` / `뜨겁게 완성되는 한 판`). 2번 카드 하단 골드 보더(활성).
  → **기존 영상 자산 `brand-videos/1~3.mp4` 재사용.** 포스터 프레임은 ffmpeg으로 추출.
- 하단 중앙 골드 아웃라인 필: `✦ | 필터 없는 주방, [최강피자의 진짜 이야기]를 지금 확인해보세요!`
- 우하단 골드 TALK 플로팅.

### S7 · 리뷰 증명 — `88` (그대로, 파랑만 밝게)
- 구조·문구 변경 없음. **중앙 네이비 카드(`#061C54`)를 `--blue-band`(#0148E3)로 교체**, 주변 배경 톤도 함께 밝힘.
- 유지: `2026년 / 고객님의 성원에 오픈 3개월 / 매출 [2억] 달성`(2억 레드) / `★★★★★ 4.9 / 5.0 만족도`.
- 배경 리뷰 캡처 17장 그대로.

### S8 · 가맹 문의 + 푸터 — `10` (그대로)
- 변경 없음. 다크 주방 사진 배경, `최강피자 [가맹 문의]`(골드), 골드 전화 카드 + 화이트 이메일 카드, 회사정보 푸터.
- 단, 위 섹션들이 밝아진 만큼 **S7→S8 전환의 명암 낙차만 재확인** 필요.

---

## 4. 코드 구조 계획

현재 `ported-contact-band.tsx` 한 파일이 962줄에 5개 섹션을 담고 있습니다. 이번에 해체합니다.

```
src/components/home/
├─ site-header.tsx                 (S0, 재작성)
├─ sections/
│  ├─ hero-section.tsx             (S1 신규)
│  ├─ franchise-benefit-section.tsx(S2 신규)
│  ├─ philosophy-section.tsx       (S3 신규)
│  ├─ profit-structure-section.tsx (S4 신규)
│  │  └─ profit-donut.tsx          (SVG 차트)
│  ├─ focus-system-section.tsx     (S5 개편)
│  ├─ real-kitchen-section.tsx     (S6 재작성)
│  ├─ review-proof-section.tsx     (S7 색만)
│  └─ contact-footer-section.tsx   (S8 이관)
├─ ui/
│  ├─ eyebrow-pill.tsx             (FRANCHISE / PHILOSOPHY 공용)
│  ├─ brush-highlight.tsx          (SVG 브러시)
│  ├─ mascot.tsx
│  └─ floating-rail.tsx            (TALK / 상담 / TOP)
└─ icons/                          (SVG 아이콘 전량)

src/lib/home-content.ts             ← ported-homepage-data.ts 대체
```

| 파일 | 조치 |
|---|---|
| `ported-hero.tsx` | 삭제 → `hero-section.tsx` |
| `ported-value-strip.tsx` | 삭제 |
| `ported-menu-section.tsx` | 홈에서 제거 (`/menu`는 별도 컴포넌트라 영향 없음 — 확인 필요) |
| `ported-contact-band.tsx` | 해체 → S3/S4/S5/S8로 분할, 샵인샵 코드 삭제 |
| `ported-review-showcase.tsx` | 유지, 색 토큰만 교체 |
| `ported-brand-video-section.tsx` | 재작성 |
| `ported-floating-kakao-button.tsx` | `floating-rail.tsx`로 확장 |
| `ported-viewport-runtime-fix.tsx` | **변경 없음** (카카오 인앱 대응, 반드시 유지) |
| `reference/*` | 미사용 여부 확인 후 정리 |
| `globals.css` | `color-scheme` light 전환, body 배경 화이트, `@theme` 토큰 레이어 신설 |

**scroll-snap 재검토:** 현재 ≥1024px에서 `scroll-snap-type: y mandatory`. 새 섹션은 높이 편차가 큽니다(S4는 매우 김). mandatory 스냅과 충돌 → **`proximity`로 완화하거나 제거 권장.**

**SEO:** `page.tsx`의 구조화 데이터 중 `#shopinshop-intro-section`, `#threeway-section` 앵커가 삭제 섹션을 가리킴 → 신규 앵커로 갱신 필요. sitemap/robots는 그대로.

---

## 5. 자산 작업 — ⚠️ 착수 전 해결해야 할 의존성

| 자산 | 상태 | 조치 |
|---|---|---|
| 히어로 반반피자 | ✅ 보유 (`hero-choigang-half-half-pizza.png`) | 그대로 |
| 실사 리뷰 17장 | ✅ 보유 | 그대로 |
| 주방 영상 3편 | ✅ 보유 (`brand-videos/`) | 포스터 프레임 ffmpeg 추출 |
| 마스코트 엄지척 | ✅ 보유 (`KakaoTalk_20260812_*_04.png` 등 고해상 7종) | **배경 제거 → 투명 WebP 변환** |
| 레드컨셉 매장 실사 | ✅ 보유 (`자료/레드컨셉.png`) | **상단 영역 크롭 → WebP 내보내기** |
| S5 원형 일러스트 4종 | ❌ **없음** | 아래 참조 |
| S4 도넛/표 아이콘 | ➖ SVG로 직접 제작 |
| S2 브러시 스트로크 | ➖ SVG로 직접 제작 |
| 라인 아이콘 전량 | ➖ SVG로 직접 제작 (시안 그램마 준수: 골드 2px, 라운드 캡) |

**S5 일러스트가 유일한 블로커입니다.** 시안 PNG는 1672px 폭이라 원형 하나당 약 200px — 레티나에서 흐립니다.
→ ① 디자이너에게 원본(PNG/SVG, 최소 800px) 요청 **[권장]**
→ ② 임시로 시안에서 크롭해 1x로 런칭 후 교체
→ ③ SVG로 재해석 (3D 렌더 질감 포기)

**폰트 원본명**도 함께 확인 요청하면 좋습니다.

---

## 6. 반응형 계획 — 시안이 데스크톱만 있으므로 명시

전 시안이 1672px 데스크톱 1종입니다. 모바일은 우리가 설계해야 하고, **실제 트래픽 다수가 모바일(카카오 인앱)** 이므로 리스크가 여기 몰려 있습니다.

| 섹션 | 모바일 전략 |
|---|---|
| S1 | 세로 스택. 피자를 상단 배경으로 올리고 헤드라인은 그 아래. 아이콘 3종 가로 스크롤 아닌 3열 압축 |
| S2 | **표 → 카드 4장 변환** (구분/내용/혜택을 카드 내 라벨-값 쌍으로). 골드 필은 카드 우상단 |
| S3 | 카드 상하 분할 — 텍스트 위, 매장 사진 아래. 마스코트 축소 |
| S4 | 도넛 먼저(폭 100%), 라벨은 조각 밖 범례로 이동. 표는 그 아래 전폭. 신뢰 카드 3종 세로 |
| S5 | 4카드 세로 스택, 연결자(`=`,`+`,`+`)를 카드 사이 수직 배치로 회전 |
| S6 | 헤드라인 위, 영상 카드 3장 가로 스와이프 |
| S7 | 기존 모바일 동작 유지 |
| S8 | 기존 유지 |

**카카오 인앱 브라우저:** `--app-height` 보정 로직이 이미 있으므로 신규 섹션도 `app-screen-*` 유틸을 그대로 씁니다. 다만 신규 섹션은 고정 뷰포트 높이가 아니라 콘텐츠 높이 기반이라 대부분 영향 없음.

---

## 7. 실행 순서

**Phase 0 — 준비**
1. 브랜치 생성 `redesign/2026-comp`. 현재 미커밋 변경 6개 파일 먼저 정리(커밋 또는 스태시).
2. 토큰 레이어 신설 (`globals.css` + Tailwind `@theme`), `color-scheme` 전환.
3. 자산 파이프라인: 마스코트 배경제거, 레드컨셉 크롭, 영상 포스터 추출.
4. 스크린샷 검증 수단 확보 (playwright 미설치 상태 — 설치 또는 impeccable `live` 모드 구성).

**Phase 1 — 재현 (comp is king)**
섹션을 시안 폭(1672px)에서 하나씩 만들고, **매 섹션마다 스크린샷을 시안 이미지와 같은 크기로 나란히 놓고 대조**합니다. 기억이 아니라 비교가 판정 기준입니다.
순서: S1 히어로 → (히어로 합격 후에만 진행) → S2 → S3 → S4 → S5 → S6 → S7 색교체 → S8 이관.

**Phase 2 — 모션·인터랙션**
도넛 순차 드로잉, 표 행 스태거 등장, 히어로 피자 진입, 영상 카드 호버·재생, 플로팅 레일 스크롤 반응, 헤더 투명→솔리드 전환. `prefers-reduced-motion` 전량 대응.

**Phase 3 — 반응형**
§6 표대로 모바일/태블릿. 카카오 인앱 실기기 확인.

**Phase 4 — 마감**
1. 데스크톱·모바일 스크린샷 1회 배치 촬영 → `.impeccable/review/`
2. `detect.mjs` 기계 검사 1회
3. `impeccable-finish-reviewer` 스폰 → 지적사항 일괄 수정 → 1회 재촬영·재판정 (최대 2라운드)
4. `impeccable-documenter` 스폰 → `DESIGN.md` 기록
5. 링크·앵커·구조화 데이터·OG 이미지 최종 점검

---

## 8. 지금 결정이 필요한 사항

1. **S5 원형 일러스트 4종** — 디자이너 원본 요청할지, 임시 크롭으로 갈지. *(블로커)*
2. **디스플레이 폰트** — 시안 원본 폰트명 확인 가능한지. 불가 시 Pretendard 900 vs 웹폰트 1종 추가.
3. **홈 메뉴 섹션** — 통합본에 없으므로 제거로 진행합니다. 유지 원하시면 알려주세요.
4. **scroll-snap** — 데스크톱 mandatory 스냅 제거 또는 proximity 완화로 진행합니다.
5. **작업 범위** — 홈(`/`)만인지, `/menu` 페이지도 새 색 체계로 맞출지.
